import React from "react";
import { styled } from "styled-components";

const Footer: React.FC = () => {
  return <MainWrap>hello</MainWrap>;
};

const MainWrap = styled.div``;

export default Footer;
