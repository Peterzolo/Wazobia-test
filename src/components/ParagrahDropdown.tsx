import React from "react";
import { styled } from "styled-components";

const ParagrahDropdown: React.FC = () => {
  return <MainWrap></MainWrap>;
};

const MainWrap = styled.div`
  width: 200px;
  height: 200px;
  background-color: lightgray;
  border-radius: 10px;
`;

export default ParagrahDropdown;
